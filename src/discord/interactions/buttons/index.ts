import ReSummarizeButton from './re-summarize';
import ReTranslateButton from './re-translate';
import SummarizeButton from './summarize';
import TranslateButton from './translate';

const allButtons = [
	new SummarizeButton(),
	new ReSummarizeButton(),
	new TranslateButton(),
	new ReTranslateButton(),
];

export default allButtons;
